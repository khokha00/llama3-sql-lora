# Core
torch>=2.2.0
transformers>=4.41.0
accelerate>=0.30.0
datasets>=2.19.0

# QLoRA fine-tuning
peft>=0.11.0
bitsandbytes>=0.43.0
trl>=0.9.0

# Config / utils
pyyaml>=6.0
tqdm>=4.66.0
sqlglot>=23.0.0        # for SQL parsing/normalization during eval
sqlparse>=0.5.0

# Hugging Face Hub
huggingface_hub>=0.23.0

# Eval harness
evaluate>=0.4.2
numpy>=1.26.0
pandas>=2.2.0

# ONNX export + quantization (Week 9)
optimum[onnxruntime-gpu]>=1.20.0
onnx>=1.16.0
onnxruntime-gpu>=1.18.0   # use onnxruntime (CPU build) instead if no GPU at inference time